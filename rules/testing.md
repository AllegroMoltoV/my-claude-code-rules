test において、下記を遵守して下さい。
- 開発は原則として TDD (テスト駆動開発) で行うこと。TDD が適さないと判断した場合は、作業を進める前に人間に相談すること。
- test に失敗したときは、場当たり的に test を修正せず、まずは実装の誤りを疑うこと。
- coverage を 100% に近づけること。
- 外部依存 (fetch や外部 API) をモックしたユニットテストは、CORS・認証・遅延といった実環境の制約を検出できないことを認識すること。モックは引数と戻り値の伝搬しか検証しないため、テストが通ったあとに必ず実環境で目視確認すること。
